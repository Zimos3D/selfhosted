version: '3.8'

services:
  traefik:
    image: traefik:v3.7.8
    restart: unless-stopped
    command:
      - "--api.dashboard=true"
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.myresolver.acme.httpchallenge=true"
      - "--certificatesresolvers.myresolver.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.myresolver.acme.email=${ACME_EMAIL}"
      - "--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json"
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
      - "--providers.file.filename=/tls-dynamic.yml"
      - "--providers.docker.endpoint=unix:///var/run/docker.sock"

    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      # Bind mount instead of named volume
      - "${DATA_PATH}/selfhosted_traefik_letsencrypt:/letsencrypt"
      - "./certs:/certs:ro"
      - "./tls-dynamic.yml:/tls-dynamic.yml:ro" 
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik-dashboard.rule=Host(`dashboard.zimos3d.com`)"
      - "traefik.http.routers.traefik-dashboard.service=api@internal"
      - "traefik.http.routers.traefik-dashboard.entrypoints=websecure"
      - "traefik.http.routers.traefik-dashboard.tls.certresolver=myresolver"

  cloudflared:
    image: cloudflare/cloudflared:2026.7.3
    restart: unless-stopped
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    command: 
      - tunnel
      - run
    volumes:
      - "${DATA_PATH}/selfhosted_cloudflared_config:/etc/cloudflared:ro"
    networks:
      - web
    labels:
      - "traefik.enable=false"

  portainer:
    image: portainer/portainer-ce:2.42.0
    container_name: portainer
    restart: unless-stopped
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      # Bind mount instead of named volume
      - "${DATA_PATH}/selfhosted_portainer_data:/data"
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portainer.rule=Host(`portainer.zimos3d.com`)"
      - "traefik.http.routers.portainer.entrypoints=websecure"
      - "traefik.http.routers.portainer.tls=true"
      - "traefik.http.routers.portainer.tls.certresolver=myresolver"
      - "traefik.http.services.portainer.loadbalancer.server.port=9000"


networks:
  web:
    name: web
    driver: bridge

# No volumes section needed—everything is bind mounted